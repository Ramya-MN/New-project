merged_df = scaled_df.merge(

    original_df[
        [
            "chat_id",
            "chat_readable"
        ]
    ],

    on="chat_id",
    how="left"

)

high_risk_chats = merged_df[

    merged_df["final_risk_label"]
    == "High Risk"

]

high_risk_chats = high_risk_chats.sort_values(

    by="final_risk_score",
    ascending=False

)

high_risk_chats[
    [
        "chat_id",
        "final_risk_score",
        "chat_readable"
    ]
]
