# Check empty token lists

empty_docs = df["tokens"].apply(len) == 0

print("Empty token documents:")
print(empty_docs.sum())

# Check empty BOW vectors

empty_bow = [len(bow) == 0 for bow in corpus]

print("Empty BOW documents:")
print(sum(empty_bow))
